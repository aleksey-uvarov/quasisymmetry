# Core numerics / plotting
numpy
scipy
matplotlib
tqdm

# FCI / statevector / Davidson paths
pyscf
ffsim
openfermion
openfermionpyscf

# DMRG (required for --reference dmrg / --backend dmrg / solve_dmrg.py)
block2

# Qubit TN helpers in src/tn.py
quimb

# Tests
pytest

# mpi4py is NOT listed here. On Alliance Canada / Compute Canada it comes from
# `module load mpi4py` (do not pip install). Elsewhere, install requirements-mpi.txt
# or let setup_venv.sh / setup_venv.ps1 install it.
